
FROM postgis/postgis:18-3.6

# Install OGR FDW for PostgreSQL 18 and minimal GDAL tools (optional)
RUN apt-get update \
 && apt-get install -y --no-install-recommends postgresql-18-ogr-fdw gdal-bin \
 && rm -rf /var/lib/apt/lists/*
